const buildRequest = (request) => {
    return providerRequest = {
        provider: "Request for Simple Provider",
        id: request.id,
        success: true,
    };
}

module.exports = buildRequest;